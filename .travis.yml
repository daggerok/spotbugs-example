env:
  global:
  - TERM=dumb

language: java
jdk: oraclejdk8

apt:
  update: true
  packages:
  - bash

install: true

script:

- ./mvnw
- ./mvnw clean spotbugs:check -Ddetail=true

- ./gradlew
- ./gradlew clean check

cache:
  directories:
  - "$HOME/.m2"
  - "$HOME/.gradle"
